apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ecloud

resources:
  - namespace.yaml
  - configmap.yaml
  - service.yaml
  - statefulset.yaml

commonLabels:
  app: tdengine
  version: "3.3.6.13"
